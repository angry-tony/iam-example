<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.opencloudengine.garuda.web.req.ReqRepository">

    <insert id="insert" parameterType="org.opencloudengine.garuda.web.req.Req">
        INSERT INTO REQ (name, email, telephone, type, customer, subject, message)
        VALUES (
          #{name}, #{email}, #{telephone}, #{type}, #{customer}, #{subject}, #{message}
        )
    </insert>

</mapper>